# 2026-07-31

> Настроение: рабочий | Сессий: 3

## Задачи и результаты

### Два HTML-отчёта Битрикс24 за одну сессию
«Проекты ОД» (группа 985) — 31 активная задача, 461 сообщение, 11 сотрудников, 3 батча → параллельный анализ 4 sub-agent'ами; второй — просроченные задачи подразделения (дерево dept 165, 24 активных сотрудника).

### Скилл `b24-tasks-dashboard` — эталон, одобрен Денисом. «Делает» в Руковожу = активные без status 4/6
сошлось 192=192, точно. Эффективность = 100 − (Замечания/Всего)×100; замечание = событие «задача просрочена» в чате, не сбрасывается при переносе срока.

### subStatus — источник истины, не status/deadline
Ждёт контроля = sub=3, Просрочена = sub=-2 (B24 ставит даже БЕЗ дедлайна — 31/51), Ожидает выполнения = sub=-3.

### Баги API коробки B24
`user.get` игнорирует START-пагинацию (брать по отделам через `filter[UF_DEPARTMENT]`); `result.tasks` — словарь, не список; `select[]` в Python-dict перезаписываются — передавать отдельными `-d`; ссылки на задачи только `/company/personal/user/{uid}/tasks/task/view/{id}/`.

- Subagent возвращает шаблонные затычки — контроль фразами-маркерами + retry с жёстким промптом.


## Инсайты и выводы

- **«Модели не существует» — опасный ответ без проверки.** Денис спросил про Qwen 3.8 Pro — я ответил «не существует», а модель анонсировали **19 июля 2026** (12 дней назад): 2.4T параметров, MoE, мультимодальная. Денис поправил. Knowledge cutoff ≠ текущая реальность; свежие релизы проверять поиском всегда.

- **Тарифный вопрос решается данными, не предположениями.** На «какой тариф тебе подойдёт» собрал реальную статистику за 30 дней (сессии, токены по провайдерам, баланс DeepSeek) — вывод: прямой DeepSeek API (V4 Pro) уже оптимален.

- **«Готово» = результат наблюдаемый у Дениса.** Вердикт по провалу Factual Gate: статические проверки («процесс жив», unit-зелёные, «зарегистрирован») ≠ работает. Рестарт gateway — платное действие, max 1 на задачу.


## Трудности и решения

- Отрицание существования без проверки — «Qwen 3.8 не существует» при живом релизе 12-дневной давности. ⚠️ Повторить недопустимо.

- Статическая верификация вместо наблюдаемого результата — корень провала Factual Gate (вердикт Дениса сегодня).


## Рекомендации на будущее

- **Перед «такого нет» — один поисковый запрос.** Цена ошибки «модель не существует» = доверие Дениса; цена проверки = 10 секунд.

- **После патчей скриптов в `~/.hermes/cache/b24_dashboard` — сразу `cp` в `scripts/` скилла**, иначе следующий прогон берёт устаревший код.

- **В HTML-рендере guard от null deadline** — JS exception молча убивает секции Просроченные/Инсайты; проверять `.task-item` count при browser-verify.

- **Провал признавать полностью и без самозащиты** — анализ причин вместо оправданий (урок Factual Gate).


## Количественные результаты

- 2 HTML-отчёта доставлены в `~/Downloads`, верифицированы в браузере перед отправкой.

- Метрика «Делает» воспроизведена точно: 192=192; «Эффективность» — с точностью ±13 п.п. (REST-метода нет, точная репликация невозможна).

- 1 новый скилл (`b24-portal-metrics`), 1 скилл получил статус эталона.


## Предложения по улучшению

- Завести мини-ритуал «свежий релиз?» перед отрицанием существования моделей/продуктов (web_search обязателен).

- Все b24-скиллы: зафиксировать pitfall «cache vs scripts рассинхрон» внутри самих скиллов (уже в MEMORY — продублировать в SKILL.md).

