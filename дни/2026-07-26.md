# 2026-07-26

> Настроение: рабочий | Сессий: 2

## Задачи и результаты

### Cron-сбои
3 джобы в ошибке: `daily-learning-journal` (413 ×2), `weekly-h-audit` (413), `rutracker-daily` (TimeoutError). Остальные джобы (maxim-daily-backup, Autonomous Solver Check, ежедневный брифинг, consistency watchdog, memory backup) — OK.

### 413 на pro-модели — новый класс ошибок. Раньше 413 возникал только на flash-моделях при переполнении контекста. Теперь — на pro с минимальным промптом. Гипотеза
байтовый лимит на уровне API-прокси DeepSeek, а не контекстное окно.

### amoCRM UI
DOM врёт про dropdown воронок. Контрольный список через DOM дал 36 воронок (8 активных + 28 архивных), но сам dropdown — React-портал: console-селекторы его не видят. Решение — открыть мышью и читать Vision (Обзвон, Продажа (Эскиз), Продажа Строительство, Согласование Эскиза, Реализация КМ, Оплата каркаса и др.).

### Координатный метод для Digital Pipeline
привязка триггеров к этапам через координаты после загрузки DP (видно 12 этапов, источники и триггеры).


## Инсайты и выводы

- **Воскресенье: утро рутинное, вечер — рабочий.** Утром 1 сессия без взаимодействия; вечером desktop-сессия на 180 сообщений — полное погружение в amocrm-project по запросу Дениса.

- **HTTP 413 на DeepSeek v4-pro — не только контекстная проблема.** `daily-learning-journal` (эта джоба) упала с 413 даже без загрузки скиллов. Корневая причина — не размер промпта, а байтовый лимит API (возможно, limit request body на уровне прокси). `weekly-h-audit` — та же ошибка.

- **state.db вырос на +96MB за сутки** (со ~37MB до 133MB). Аномалия: порог 20MB/день превышен в 5 раз. Требует расследования источника раздувания и VACUUM.

- **Фаза 1 аудита amoCRM = сухие факты без интерпретаций.** Я — датчик, не консультант: названия, количества, значения списков, тексты триггеров. Оценки («зрелость», «логика процесса») — только Фаза 2.

- **скилл `amocrm-project` рассинхронизирован с диском.** Скилл описывает v4 (`~/Downloads/amocrm-project-v4/`), реально существует только v1 (`amocrm-project`). Прочитано ~110 md-файлов проекта: принципы, ТЗ, инструкции Фаз 1–2.


## Рекомендации на будущее

- **При 413 на pro — проверять байтовый размер запроса,** а не только токены. Возможно, проблема в загрузке бинарных данных или слишком длинных system-промптов.

- **Dropdown в amoCRM — сначала мышь + Vision, потом DOM.** Не тратить попытки на console-селекторы React-порталов.

- **Обновить скилл `amocrm-project`:** убрать ссылки на несуществующий v4, зафиксировать реальную структуру v1.

- **state.db VACUUM — сделать сегодня.** +96MB за сутки аномально; если тренд сохранится, БД достигнет 500MB за 4 дня.

